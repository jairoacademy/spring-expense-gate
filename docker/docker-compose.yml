version: "3.9"
services:
  postgres:
    image: postgres:16-alpine
    container_name: expensegate-db
    environment:
      POSTGRES_DB: expensegate
      POSTGRES_USER: expensegate
      POSTGRES_PASSWORD: expensegate
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
volumes:
  postgres_data:
    driver: local